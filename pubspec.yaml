name: webim
description: "A new dart package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.7.0

dependencies:
  drift: ^2.30.0
  flutter:
    sdk: flutter
  http: ^1.6.0
  path_provider: ^2.1.0
  sqlite3: ^2.5.0
  web_socket_channel: ^3.0.3
  get_it: ^9.2.1

dev_dependencies:
  build_runner: ^2.4.13
  drift_dev: ^2.30.0
